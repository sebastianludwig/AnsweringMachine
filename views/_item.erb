<li>
  <%= "#{item.path} (#{item.content_type}) @#{item.http_status} #{item.requested_at.to_s}" %><br />
  <form action="/resend/<%= item.id %>" method="post">
    <input type="submit" name="_method" value="put"/>
  </form>
  <form action="/response/<%= item.id %>" method="post">
    <input type="submit" name="_method" value="delete"/>
  </form>
  <%= "#{item.body}" %>
</li>